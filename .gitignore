node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env / secrets — repo is PUBLIC, never commit these
.env
.env.*
!.env.example
!.env.test
.dev.vars
.dev.vars.*

# Local D1 / SQLite
*.sqlite
*.sqlite3
*.db

# Generated by `wrangler types` (run automatically by the `prepare` script).
# Deliberately NOT committed: wrangler emits an extra `mainModule` line when the
# adapter's build output exists, so the file legitimately differs before and after
# a build. A committed copy would flip CI red depending on step order.
worker-configuration.d.ts

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
# Playwright
test-results
